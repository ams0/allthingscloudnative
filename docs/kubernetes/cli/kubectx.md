# Kubectx

[Kubextx and kubens](https://github.com/ahmetb/kubectx#kubectl-plugins-macos-and-linux) are useful commands to switch between contexts and namespaces.

They're best deployed as krew plugins:

```bash
kubectl krew install ctx
kubectl krew install ns
```